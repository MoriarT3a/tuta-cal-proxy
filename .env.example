# Konfiguration für den ICS Calendar Proxy
# Diese Datei in eine Datei namens ".env" kopieren und anpassen

# Die Quell-URL des Kalenders, der umgewandelt werden soll
SOURCE_CALENDAR_URL=https://my-calendar.ics

# Port des Webservers (Standard: 8098)
PORT=8098

# Logging-Level (INFO, DEBUG, WARNING, ERROR)
LOG_LEVEL=INFO

# Zeitzone (Standard: Europe/Berlin)
TZ=Europe/Berlin
